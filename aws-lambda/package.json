{
  "name": "snippets-api",
  "version": "1.0.0",
  "description": "AWS Lambda API for Weekly Snippets Reminder",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "deploy": "sam build && sam deploy",
    "local": "sam local start-api",
    "test": "jest"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "aws-lambda": "^1.0.7",
    "@types/aws-lambda": "^8.10.0",
    "jsonwebtoken": "^9.0.0",
    "node-fetch": "^3.3.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0",
    "@types/jsonwebtoken": "^9.0.0",
    "prisma": "^5.0.0",
    "jest": "^29.0.0",
    "@types/jest": "^29.0.0"
  }
}